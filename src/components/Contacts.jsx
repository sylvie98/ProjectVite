import React from 'react'
import { Link } from 'react-router-dom'
const Contacts = () => {
  return (
    <div>
          <Link to={"/Contact"}>
          <a>Contact</a>
        </Link>
    </div>
  )
}

export default Contacts